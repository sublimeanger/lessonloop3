# LessonLoop — Claude Code Context
Last updated: 4 April 2026

## Project Overview
LessonLoop is a music academy management SaaS. React 18 / TypeScript / Vite frontend, Supabase backend (PostgreSQL, Auth, Edge Functions, Storage, Realtime), Stripe Connect for payments, Anthropic Claude for the LoopAssist AI copilot.
**Repo:** github.com/sublimeanger/lessonloop3
**App:** https://app.lessonloop.net
**Marketing:** https://lessonloop.net
**Supabase project:** ximxgnkpcswbvfrkkmjq.supabase.co

## Architecture
### Two-Site Setup
| Site | URL | Stack | Hosting |
|------|-----|-------|---------|
| App | app.lessonloop.net | React SPA | Lovable |
| Marketing | lessonloop.net | Static HTML (prerendered) | Cloudflare Pages |
The marketing site is generated by `node scripts/prerender.mjs`. **Critical:** always verify no `localhost` references in output HTML after builds — there was a past incident where `localhost:5173` leaked into production.

### Native Apps
- iOS: v1.2 submitted to App Store 3 April 2026, awaiting review. v1.3 pending after approval (25-file mobile sweep: DatePicker, dialog, touch fixes).
- Android: AAB built for Play Store
- Built with Capacitor
- **Use `npx cap copy ios` NOT `npx cap sync`** — sync hangs
- Platform detection: `src/lib/platform.ts`
- Stripe/Apple IAP conflict: payment UI hidden in native context

### Division of Labour — STRICT
| Tool | Owns | Never Touches |
|------|------|---------------|
| **Lovable** | React components, pages, hooks, styling, UI/UX, edge function deployment | Edge function logic, migrations, RLS, RPCs |
| **Claude Code** | Edge functions, migrations, RLS, RPCs, test infra | UI components, pages, hooks, styling |
This separation is absolute. Commits must respect it.

### ⚠️ CRITICAL: Lovable + Database Migrations
Lovable is connected to Supabase and deploys frontend + edge functions automatically. However, **Lovable does NOT reliably apply Claude Code's SQL migrations**. When Claude Code creates new migrations:
1. Migrations exist in `supabase/migrations/` in the repo
2. Lovable may bundle some into its own deployment but frequently skips Claude Code migrations
3. **After ANY Claude Code session that creates migrations, Jamie must manually apply them via Supabase SQL Editor**
4. Always remind Jamie: "New migrations were created — these need applying to the live Supabase DB via SQL Editor or `supabase db push`"
5. After applying, run `NOTIFY pgrst, 'reload schema';` in SQL Editor
6. All migration SQL must be idempotent: use `IF NOT EXISTS`, `CREATE OR REPLACE`, `DROP ... IF EXISTS` before `CREATE`, and wrap `ADD CONSTRAINT` in `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;`
7. CHECK constraints on existing tables must use `NOT VALID` to avoid failing on legacy data

### ⚠️ CRITICAL: Lovable Edge Function Deployment
When Lovable shows correct edge function code but the function doesn't work in production, the deployed version may be stale. **Always force a fresh redeployment** if behaviour doesn't match the code. This was confirmed as a real issue during the Xero integration (April 4, 2026).

## Database
### Key Tables (70+ total)
**Auth & Orgs:**
- `organisations` — 62+ columns. Timezone (IANA validated), currency_code (ISO 4217 validated), subscription fields (protected by `protect_subscription_fields` trigger), branding, continuation settings, payment plan defaults, dunning/reminder configuration.
- `org_memberships` — role ENUM (owner, admin, teacher, finance, parent). org_id immutability trigger. **NEVER reference `organisation_members` — the correct table is `org_memberships`.**
- `profiles` — FK to auth.users

**Academy:**
- `students` — soft-delete (archived). Guardian linking via `student_guardians`. Notes field restricted from parents at RPC level.
- `teachers` — dual linking: `teacher_user_id` (direct) and `teacher_id` → `teachers.user_id` (indirect). `is_lesson_teacher()` and `can_edit_lesson()` check both paths. `pay_rate_type` (fixed/percentage) and `pay_rate_value` for payroll.
- `locations` — timezone per location, rooms with capacity. Unique name per org (case-insensitive). Primary location via `set_primary_location` RPC.
- `lessons` — CHECK(end_at > start_at NOT VALID). FK to teacher, location, room. `is_open_slot` flag. Cannot delete if invoiced (trigger guard).
- `lesson_participants` — `rate_minor` snapshot (captured at lesson creation, preserved on edit). `is_open_slot` auto-cleared on INSERT.
- `lesson_notes` — accessed via RPCs only (`get_lesson_notes_for_staff`, `get_parent_lesson_notes`). Column-level privacy: `teacher_private_notes` never exposed to parents.
- `recurrence_rules` — weekly recurrence with DST-safe generation via date-fns-tz. 200-lesson cap.
- `external_busy_blocks` — Google Calendar busy block data for teacher calendar overlay.

**Billing:**
- `billing_runs` — FK to invoices via `invoices.billing_run_id`. Delete RPC (`delete_billing_run`) with paid invoice guard.
- `invoices` — status lifecycle: draft → sent → paid → voided. DB trigger enforces valid transitions. `total_minor`, `paid_minor` in integer minor units. Payment plan support (`plan_enabled`, `installment_count`, `installment_frequency`).
- `invoice_items` — CHECK constraints (NOT VALID): unit_price_minor >= 0, amount_minor >= 0, quantity > 0.
- `payments` — unique constraint on Stripe payment intent ID. Three-layer idempotency.
- `refunds` — `voided_at` + `voided_by`. RLS: no INSERT/UPDATE/DELETE for authenticated users (service_role only via edge functions). `record_manual_refund` RPC for non-Stripe refunds.

**Xero Integration:**
- `xero_connections` — org-level Xero OAuth tokens, tenant mapping, sync preferences. RLS: org admins only.
- `xero_entity_mappings` — maps LessonLoop entity IDs (invoices, contacts) to Xero IDs. Used for sync deduplication.

**Continuation & Credits:**
- `terms` — start/end dates per org
- `term_continuation_runs` / `term_continuation_responses` — status lifecycle with server-side deadline enforcement. `recalc_continuation_summary` RPC.
- `make_up_credits` — `voided_at` column used throughout (available_credits view, all RPCs filter `WHERE voided_at IS NULL`). Consumption via FOR UPDATE SKIP LOCKED. Max per term enforced. Credits on active waitlist don't expire. `void_make_up_credit` RPC. Auto-issued via `trg_auto_credit` trigger on attendance.
- `make_up_waitlist` / `enrolment_waitlist` — duplicate prevention index. Mutual exclusion on booking. Atomic conversion RPCs. `void_credits_on_student_delete` trigger.

**Pipeline:**
- `leads` / `lead_students` / `lead_activities` / `lead_follow_ups` — lead pipeline. `convert_lead` RPC for atomic conversion. Booking page enquiry-only submissions feed into leads.
- `booking_pages` / `booking_page_teachers` / `booking_page_instruments` — public booking. Supports both full booking and enquiry-only mode.

**Messaging:**
- `internal_messages` — threaded conversations. `payment_notifications` INSERT restricted to service_role only.
- `notification_preferences` — marketing emails default to opt-out (GDPR compliant).

### Key RPCs (verified live April 2026)
set_primary_location, bulk_update_lessons, bulk_cancel_lessons,
record_manual_refund, void_make_up_credit, get_lesson_notes_for_staff,
get_parent_lesson_notes, convert_lead, void_credits_on_student_delete,
recalc_continuation_summary, auto_issue_credit_on_absence, query_org_data (LoopAssist analytics)

### RLS Patterns
Every table has RLS enabled. Key patterns:
1. **Org isolation:** all queries scoped by org_id via org_memberships lookup
2. **Role-based:** `is_org_admin()`, `is_org_staff()`, `is_lesson_teacher()`, `is_parent_of_student()`
3. **SECURITY DEFINER RPCs:** ALL must verify `auth.uid()` + org membership at the top. No exceptions.
4. **Never use `USING(true)` or `WITH CHECK(true)`** on sensitive tables
5. **Column-level privacy:** lesson notes use RPCs to exclude `teacher_private_notes` from parent responses
6. **Parent portal guardian chain:** auth.uid() → guardians.user_id → student_guardians.guardian_id → students
7. **All SECURITY DEFINER functions** callable from frontend MUST have `is_org_*` auth check as first statement
8. **PostgreSQL grants EXECUTE to PUBLIC by default** — always add either internal auth check or `REVOKE EXECUTE FROM authenticated`

## LoopAssist v2 (deployed March 2026)
AI copilot using Anthropic Claude API with streaming.
- **knowledge-base.ts** (489 lines) — comprehensive structured product knowledge at `supabase/functions/looopassist-chat/knowledge-base.ts`
- **Prompt caching:** `cache_control: { type: "ephemeral" }` on static KB block (Block 1). 90% cost reduction.
- **Native streaming:** `stream: true` + `consumeAnthropicStream()`. Null body guard on stream consumer.
- **Model routing:** Haiku 4.5 for free/teacher/studio, Sonnet 4.5 for academy/agency/custom. Model version pinned with documentation comment.
- **max_tokens:** 1024. **Daily cap:** 200/org/day.
- **Architecture:** Block 1 (cached): knowledge base. Block 2 (dynamic): context. NEVER put timestamps or per-request data in Block 1.
- **Update product knowledge** by editing knowledge-base.ts without touching index.ts.
- **Tool usage guidance** and **CSV import + rate snapshot explanation** included in KB.
- **query_org_data tool** — queries live organisation data for analytics (student counts, lesson stats, revenue, attendance rates).

## Edge Functions (87+)
### Critical Edge Functions
| Function | Purpose | Auth | Notes |
|----------|---------|------|-------|
| `create-billing-run` | Generate invoices | JWT + admin | Rate limited. Dedup. Auto-syncs to Xero if connected. |
| `stripe-webhook` | Process Stripe events | Stripe signature | Returns 500 on DB failure. Auto-syncs payments to Xero. |
| `stripe-subscription-checkout` | Manage subscription | JWT + **owner only** | Admin NOT allowed |
| `send-invoice-email` | Email invoice | JWT + org member | Branded. Payment plan installments. |
| `create-continuation-run` | Start re-enrollment | JWT + admin | |
| `bulk-process-continuation` | Server-side continuation | JWT + admin | |
| `looopassist-chat` | AI copilot | JWT + org member | v2 streaming + KB |
| `send-bulk-message` | Org-wide messages | JWT + admin | escapeHtml. List-Unsubscribe. |
| `send-lesson-reminders` | Automated lesson reminders | service_role (cron) | 290 lines. Hourly via cron job 14. Deduplicates. |
| `send-absence-notification` | Notify parents of absence | JWT + staff | Triggered on attendance marking. |
| `calendar-refresh-busy` | Google Calendar sync | service_role (cron) | Every 15 min via cron job 15. |
| `booking-submit` | Public booking/enquiry | No auth (public) | Handles both booking and enquiry-only mode. |

### Xero Integration Edge Functions
| Function | Purpose | Auth | Notes |
|----------|---------|------|-------|
| `xero-oauth-start` | Initiate OAuth flow | verify_jwt: false | Redirects to Xero consent screen |
| `xero-oauth-callback` | Handle OAuth callback | verify_jwt: false | Exchanges code for tokens, stores in DB |
| `xero-disconnect` | Remove Xero connection | verify_jwt: false | Clears tokens and mappings |
| `xero-sync-invoice` | Sync invoices to Xero | verify_jwt: false | Creates contacts if needed |
| `xero-sync-payment` | Record payments in Xero | verify_jwt: false | **BLOCKED** until `accounting.payments` scope available |

**Xero shared helper:** `supabase/functions/_shared/xero-auth.ts`

**Xero OAuth scopes (confirmed working):**
```
openid profile email offline_access accounting.contacts accounting.invoices
```

**⚠️ Xero Scope Rules (post-March 2, 2026 apps):**
- The broad scope `accounting.transactions` is **completely unavailable** — causes `invalid_scope` error
- Use granular scopes only: `accounting.invoices`, `accounting.payments`, `accounting.contacts`, etc.
- `accounting.payments` is **not yet available** for new apps (expected end of April 2026)
- `accounting.contacts` and `accounting.settings` are unchanged and work as before
- Xero Client ID: `383EE6AE69FF4B86A4DBBA68F0DFAB47`
- Redirect URI: `https://ximxgnkpcswbvfrkkmjq.supabase.co/functions/v1/xero-oauth-callback`

### Cron Jobs
| Job | Function | Schedule | Auth |
|-----|----------|----------|------|
| 14 | send-lesson-reminders | Hourly | service_role_key |
| 15 | calendar-refresh-busy | Every 15 minutes | service_role_key |

### Edge Function Conventions
1. **Auth:** verify JWT, check org membership, check role
2. **Rate limiting:** use `_shared/rate-limit.ts`
3. **Error handling:** always return appropriate HTTP codes (never 200 on failure)
4. **Financial amounts:** always from DB, never from client
5. **Email:** Resend API, 3x retry, escapeHtml(), domain `lessonloop.net`
6. **config.toml:** email link/cron functions and all Xero functions MUST have `verify_jwt = false`

## Frontend

### Route RBAC (updated April 2026)
| Route | Allowed Roles |
|-------|--------------|
| `/settings` | owner, admin |
| `/dashboard` | owner, admin, teacher, finance |
| `/calendar` | owner, admin, teacher |
| `/invoices` | owner, admin, finance |
| `/teachers` | owner, admin |
| `/reports/payroll` | owner, admin, teacher, finance |
| `/portal/*` | parent |

### RouteGuard Fallback
When role grace period expires and `currentRole` is null, fallback redirects to `/dashboard` (NOT `/portal/home`). Only resolved `parent` role goes to `/portal/home`.

### Parent Portal Child Filter
- `ChildSwitcher` — only shows for parents with 2+ children
- URL search params (`?child=xxx`) via `useChildFilter()` context
- **Filters by child:** PortalHome, PortalSchedule, PortalPractice, PortalResources, PortalInvoices, PortalContinuation
- **No filter (by design):** PortalMessages (guardian-scoped), PortalProfile

### Parent Portal Features (April 2026)
- **Reschedule/cancel requests:** LessonChangeSheet.tsx on PortalSchedule — parents submit requests with reasons, teachers/admins approve
- **Practice tracking:** Weekly Goal ring, streak counter, milestone celebrations
- **Guardian invitation badges:** "Portal Active" (green) / "Invite Pending" (amber)
- **Branded invitation email:** Feature highlights (view schedule, track progress, manage payments)

### Calendar
- **Time offset fix (April 2026):** getAccurateY was double-counting scrollTop. Fixed in WeekTimeGrid, DayTimelineView, useDragLesson, useResizeLesson.
- **Google Calendar busy blocks:** BusyBlockOverlay.tsx, ExternalEventsSyncBadge.tsx, useExternalBusyBlocks.ts — visual overlay of external events
- All datetime comparisons use explicit UTC
- View range uses org timezone via fromZonedTime/toZonedTime
- Closure dates shown visually on all calendar views
- Past date lesson creation shows warning (not blocker)

### Settings
- **AccountingTab.tsx** — Xero Connect/Disconnect UI, sync stats, auto-sync toggles
- **InvoiceSettingsTab** — Payment Reminders section for dunning configuration
- **Teacher availability** — "My Availability" card on teacher dashboard for self-service

### Public Booking Page
- BookingPage.tsx supports both full booking and enquiry-only mode
- "Just send an enquiry" button skips slot selection
- embed.js default text: "Book a Lesson or Enquire"

### Reports
- All 8 report pages have CSV export functionality

### Key Conventions
- **Currency:** `formatCurrencyMinor(amount, currency)` — never hardcoded £/$
- **Timezone:** org timezone via date-fns-tz — never browser timezone for business logic
- **Forms:** Zod validation, `isPending`/`isSaving` double-click protection
- **Role gating:** `isOrgAdmin`, route guards, component-level checks
- **Touch targets:** 44px minimum on mobile

### Supabase API Calls from Node.js
All Supabase API calls from Node.js (in E2E tests) use `curl` via `child_process.execSync`. Do NOT use the Supabase JS client in Node test helpers.

## Testing
### Playwright E2E Suite
- 950+ tests across 24 test files
- Runs against live site via container proxy
- **Never use `waitUntil: 'networkidle'`** — causes hangs
- Test accounts: patrick-{owner,admin,teacher,finance,parent,parent2}@test.lessonloop.net

### Quality Gates
Always run before pushing:
```bash
npm run typecheck && npm run build
```

## Commit Discipline
- Descriptive commit messages: `fix(billing): add auth check to get_unbilled_lesson_ids`
- Feature prefix: `feat(calendar):`, `fix(invoices):`, `audit(students):`
- Never commit `.env` files
- Separate concerns: backend changes and frontend changes in separate commits when possible

## Audit History (March-April 2026)
### Mega Audit — 107 findings, all fixed
CM-1→7, FIN-1→5, AUTH-1→3, CAL-1→4, MSG-1→2, PPT-1→2, PIP-1→3, SET-1→3

### QA Tester Report — 50 bugs, all resolved
18 already fixed, 8 by design, 13 backend (migrations), 13 frontend (Lovable), calendar time offset, child filter + LoopAssist iOS.
- 70 migrations applied via SQL Editor (batches 0a–4)
- Subscription management restricted to owner-only

### Competitive Audit — 23 issues, all addressed (4 April 2026)
Benchmarked against My Music Staff, Opus1, Teachworks, Jackrabbit Music, Fons, Duet Partner. 19 fully implemented, 2 already handled, 2 deferred for user testing. Key additions: automated lesson reminders, Xero integration, parent reschedule requests, Google Calendar busy blocks, enquiry form, CSV exports, percentage-based teacher pay, LoopAssist real-data analytics.

## Stripe
- **Subscription management:** owner-only
- **Checkout:** embedded PaymentDrawer (portal) or redirect (admin)
- **Webhooks:** signature verification, three-layer idempotency
- **Connect:** destination charges. Refunds through platform account.
- **Payment reminders:** correctly calculate remaining balance (`total_minor - paid_minor`)
- **Invoice emails:** custom branding + payment plan installment schedules

## Remaining Items
- [ ] **Apply April 4 SQL migrations** — xero tables, dunning config, external_busy_blocks, percentage pay, hint_completions. Apply via Supabase SQL Editor + `NOTIFY pgrst, 'reload schema'`
- [ ] **iOS v1.3** — submit after v1.2 approval (25-file mobile sweep)
- [ ] **Xero `accounting.payments` scope** — revisit end of April 2026 when Xero completes granular scope rollout
- [ ] **Xero demo org testing** — create demo company in Xero, test full invoice sync round-trip
- [ ] Marketing site prerender rebuild
- [ ] Sentry source maps
- [ ] Cloudflare WAF + CSP headers
- [ ] GDPR disclosure for Anthropic as LoopAssist data sub-processor
- [ ] Android Play Store submission (AAB already built)
