/**
 * TourTrigger - DISABLED
 * 
 * Auto-triggering tours has been disabled to prevent disruption.
 * Users can manually start tours from Settings > Help & Tours.
 * 
 * To re-enable auto tours in the future, restore the previous implementation
 * that checked route paths and completion status.
 */
export function TourTrigger() {
  // Tours are now opt-in only - no auto-triggering
  return null;
}
