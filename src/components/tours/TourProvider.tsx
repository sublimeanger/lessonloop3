/**
 * TourProvider — REMOVED
 *
 * react-joyride has been removed. Guided help is now provided by the
 * contextual hint system (useContextualHints / ContextualHint component).
 *
 * This file is kept only for import compatibility during the transition.
 * It re-exports nothing — any remaining imports should be cleaned up.
 */

export type TourName = 'dashboard' | 'calendar' | 'students' | 'invoices' | 'loopassist';
